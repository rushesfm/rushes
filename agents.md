# Agent Instructions

- When the user requests new implementations, skip running type checks unless they explicitly ask for it.
- Create a git commit when the user adds the flag --git to their request.
